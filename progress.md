# Progress

## 2026-08-03（视频编辑器帧播放器）
- 开始 Phase 38：读取用户提供的 README，定位 dist 资源/API 与当前视频编辑器播放器、帧列表、动画组创建链路。
- 已确认目标库与核心 API，继续读取 README 的 dist/CDN 章节及 VideoEditorDialog 未截断的帧列表/动画组回调。
- README 已完整读取；没有 CDN 文件说明。已定位现有 dots 菜单、固定 0-0 新建组和主播放器区域，下一步查询 npm dist 并检查项目 vendor loader 约定。
- npm 确认当前稳定版 2.2.0 和主 ESM 入口；项目已有本地 vendor ESM loader。下一步检查 dist 是否含相对 import/worker 依赖并下载所需文件。
- 已确认主入口依赖一个相对 chunk；将下载固定 2.2.0 的两个 dist 文件，并通过本地 HTTP URL 原生 dynamic import，避免 Blob URL 破坏相对路径。
- 已确认核心 chunk 的 worker 均内联且提供 destruct 生命周期；所需 vendor 文件范围收敛为入口+单一 chunk。
- Phase 38 分析完成：确定本地 dist 加载、通用播放器生命周期、持久选区和 UI 重构方案。
- 视频编辑器组合补丁因 canvas 默认数据上下文未命中而未应用，改为按文件拆分补丁。
- Phase 39 完成：vendor dist、本地 loader、通用 FrameSequencePlayer 及兼容导出已实现。
- Phase 40 首轮完成：持久帧选区、点击边界、主预览 Tabs、按选区新建动画组和文档更新已实现；首轮静态检查通过。
- 本地 dist 原生导入验证通过；进入最终审查修正和帧选区行为回归覆盖。
- 选区纯函数 5 项测试通过；本地 Web 路由可访问两个 vendor JS；全部定向静态检查通过。
- Phase 41 完成：清理过时说明，补 vendor 来源/哈希记录，最终测试、编译、ESM 导入与 diff check 全部通过。

## 2026-08-03（视频秒间隔抽帧）
- 开始 Phase 35：新增“每 N 秒抽取一张”模式，保留正在进行的分组运行所有任务状态。
- Phase 35 完成：新增 seconds 模式、secondsInterval 参数、UI 小数输入、插件时间采样分支，并同步默认值、交接文档与插件双副本。
- Phase 36 静态检查完成，准备使用用户视频执行真实插件验证。
- Phase 36 完成：15.084 秒视频按 0.5/1/2 秒间隔分别输出 30/15/8 张，临时产物已清理。
- Phase 37 完成：秒间隔抽帧准备交付；未改动并行中的分组运行所有阶段状态。

## 2026-08-03（分组运行所有）
- 开始 Phase 32：为 GroupExecutionToolbar 增加全部 runs 执行编排和素材缩略图状态标签。
- 已确认现有“批量运行”只执行当前活动实例且不会等待整组完成，不能直接用于跨 run 切换。
- Phase 32-33 完成：运行所有按 runs 串行、run 内节点并行；每轮保存 nodeStates，最终恢复原 activeId；运行期间锁定所有实例编辑操作。
- Phase 34 完成：素材缩略图新增 queued/running/done/error 标签；18 项相关测试、4 文件 Babel 编译和 diff check 通过。

## 2026-08-03
- 开始 Phase 29：新增“每 N 帧抽取一张”模式，计划用源视频 362 帧验证 N=2 输出 181 张。
- Phase 29 完成：新增 interval 模式、默认 interval=2、UI 整数输入、插件 action 元数据与 select 滤镜分支，并同步交接文档和两份插件。
- Phase 30 静态检查完成：语法、Babel、双副本一致性及 diff check 全部通过，准备真实插件验证。
- Phase 30 真实验证完成：362 帧视频使用 interval=1/2/3 分别输出 362/181/121 张，临时产物已清理。
- Phase 31 完成：准备交付。
- 开始 Phase 25：排查视频编辑器“按帧、1fps”拆分 15 秒视频仅输出 15 张的问题。
- 初步确认现有 fps 模式语义为“每秒抽 N 帧”，所以 1fps × 15 秒 = 15 张；继续核对 UI 与源视频帧率，判断用户期望是否为逐原始帧导出。
- ffprobe 实测用户视频为 24fps、362 帧；确定新增“全部原始帧”模式，并澄清 fps 文案，不改变原有 count/fps 行为。
- 已确认插件模板与运行时副本一致，进入模式枚举、UI 和 ffmpeg all 分支的最小修改。
- Phase 26 实现完成：新增全部原始帧模式并澄清 fps 文案；静态检查、双副本一致性和 diff check 通过，准备执行真实插件验证。
- Phase 27 完成：真实插件调用中 all=362 帧、fps1=15 帧，结果与 ffprobe 元数据一致；临时测试目录已删除。
- Phase 28 完成：完成变更范围复核并准备交付。
- 开始 Phase 19：移除 NodeShell 上传区开关，并为分组内节点接入“应用到其他节点”属性复制流程。
- Phase 19 完成：已确认上传折叠完整链路、现有属性对话框协议、分组层级和人工/连线/分组素材的数据边界；进入实现。
- Phase 20 完成：移除上传区折叠 UI/状态/provider；分组节点顶部工具栏新增属性应用入口；对话框目标改为最小包含分组下的其他节点；人工上传图应用会保护分组素材。
- 首轮验证通过：剪贴板相关 5 项测试通过，6 个受影响 JSX/Hook 文件 Babel 编译通过，diff check 通过。
- Phase 21 完成：相关 26 项测试全部通过，旧上传折叠实现无残留，交接约束已更新；localhost:3000 当前未响应，未做真实浏览器验证。
- 用户纠正目标范围；开始 Phase 22，改为对“按上传素材执行”的其他素材实例同步当前节点属性。
- Phase 22-23 完成：撤回同组其他节点写入；入口改由 useGroupExecution 管理，只对其他素材 runs 的同一 nodeId 应用属性，并同步素材模板。
- Phase 24 完成：相关 27 项测试通过，7 个 JSX/Hook 文件 Babel 编译通过，diff check 通过；素材图片保护回归已覆盖。
- 开始实现组级输出自动绑定；已追加 Phase 12-15，进入参考实现与数据链路定位。
- 已确认参考拖线机制和现有组执行入口；组执行配置随 groups 自动持久化，准备定位输出提取与素材 run 创建逻辑。
- 已完成首轮实现；5 个受影响 JSX/Hook 文件通过 Babel 编译，diff check 通过。纯函数测试首次因既有无扩展名 ESM import 未执行，已定位并修正。
- 第二轮 7 项测试和编译检查通过；已补矩形命中、持久组连线展示、循环绑定阻止及来源组删除后的自动断开。
- 根目录 ESLint 因仓库 flat config 仅位于 packages/web 而未执行，开发服务 3000 健康检查返回 200。
- 从 Web 包定向执行 ESLint 为 0 error，但目标文件因超出配置 base path 被忽略，故不计为有效 lint 验证。
- 最终新增绑定持久化与上传槽位应用断言；共 9 项测试通过，5 文件编译通过，diff check 通过。
- Phase 12-15 完成：组拖线、过滤对话框、持久化、当前输出自动同步、断开/删源恢复、循环保护和持久连线展示均已实现。
- 开始 Phase 16：根据用户附件定位过滤对话框关闭状态的 null.filter 崩溃。
- Phase 16 完成：新增空状态过滤器解析和回归断言；8 项绑定测试、2 文件编译及 diff check 全部通过。
- 开始 Phase 17：确认 mini-app renderer 的 react-dom 映射缺少 createPortal，准备移除该运行时依赖。
- Phase 17 完成：移除 react-dom/createPortal，增加拖线起止调试日志和 renderer 兼容回归测试；9 项测试、工具栏编译及 diff check 通过。
- 开始 Phase 18：修复目标组工具栏连线手柄无法作为拖线落点的问题。
- Phase 18 完成：目标输入手柄增加 6px 命中容差，共享组输出手柄接入 onConnectGroup；10 项测试、共享 TSX/mini-app JSX 编译及 diff check 通过。
- 已阅读 handoff 与相关宿主/mini-app 源码。
- 已确定采用配置驱动的通用 Host Slot + React Portal 方案。
- 已创建任务计划，开始补齐端到端数据路径。
- 已新增 `agentChatPlacement` 的 server/sdk/preview 数据路径并在 game-asset-canvas manifest 启用。
- 已新增通用 Host Slot 注册表以及 `window.AgentSpaces` 注册、状态同步 API。
- 已将 `MiniAppAgentDock` 通过 Portal 接入 RightPanel 的 Chat tab。
- 差异复核组合命令因引号转义失败一次，已记录，待用独立命令继续。
- Server 与 SDK TypeScript 检查通过；SDK build 通过。
- Web 定向 ESLint 0 error；Web 全量类型检查中本次相关错误已清零，仍有仓库既有错误。
- mini-app 三个 JSX 入口/组件经 Babel 编译检查通过。
- Host Slot 生命周期脚本通过，覆盖重载后的激活恢复。
- 现有 Web 服务 `http://localhost:3000` 健康检查返回 200；第二实例受 Next 开发锁限制未启动。
- 开始根据 session `d61439a1-5e6d-48bd-89b3-27e3030d24d0` 分析并优化 game-asset-canvas tools。
- 已完成 session 基础统计：4 条消息、55 次工具调用，开始定位批量调用和布局异常。
- 已定位三类根因：自由对象被 `$text` 包装后假成功、增量分组布局重叠、删除节点遗留分组成员。
- 已完成首轮修复与纯函数测试：数据解包、分组并发累积、删除成员清理、批量节点即时快照；10 项测试通过。
- 已补 API 集成重放测试，针对性测试累计 12 项全部通过。
- Shared/Server 正式构建通过；从 server dist 真实加载 game-asset-canvas tools/API 并重放 session 输入通过。
- Mini-app 相关源码 Babel 编译通过；最终目标测试 10 项通过，diff check 通过。
- 工具优化完成：核心 API 会逐次热加载；tools schema 需服务重启后刷新缓存。
- 开始排查 connect_batch 创建计数与 ReactFlow 可见 edge 不一致问题。
- 已确认根因：connect_batch 创建的 edge 缺少唯一 id，持久化中 8 条边均为 id=undefined。
- 已确认手动 addEdge 与批量裸 edge 构造的实现差异，确定增加统一 edge ID 规范化与旧数据迁移。
- 已从续作上下文恢复 Phase 9，复核 planning-with-files 约束及已有根因记录，准备开始最小修复。
- Phase 9 诊断完成：三个批量边入口共用 `prepareBatchEdges`；开始 Phase 10，新增统一 edge ID 规范化并接入创建和加载路径。
- 已新增 `canvas-edges.js` 与 3 项测试；批量边创建会补 ID，画布初次加载及远端同步会规范化缺失/重复 ID 并回写。
- 复核后补强：新边 ID 会避开所有现有 edge ID；远端同步在本地 dirty 时不执行迁移回写；edge 工具测试增至 4 项。
- 9 项针对性测试通过；指定 canvas.json 的 8 条边规范化后 uniqueIds=8、missingIds=0；diff check 通过。
- 两个受影响 hook 的 Babel 编译通过，现有 3000 开发服务健康检查为 200；环境无 procm-mcp，未执行持久服务重启。
- Phase 10-11 完成：批量边创建、旧数据加载迁移、指定 8 条边唯一 ID 验证均已覆盖。
