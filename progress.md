# Mini App Launch Params Progress

## Session Log
- Created task tracking files for mini-app launch parameter work.
- Read renderer docs, TTS entry/components, host API, and podcast panel/hook to confirm current launch and event wiring.
- Added TTS launch parameter parser, launch service, startup auto-run flow, and podcast panel "发送到配音" button.
- Verified target JS/JSX syntax with esbuild transform and ran `pnpm --filter @agent-spaces/web exec tsc --noEmit --pretty false` successfully.
