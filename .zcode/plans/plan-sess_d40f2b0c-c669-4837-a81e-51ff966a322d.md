## 任务
1. 按功能拆分 `RightPanel.jsx`（725 行）为 `right-panel/` 目录下的独立文件。
2. 重写「节点管理」tab：参考 `workflow-node-list-panel.tsx` 的 **连通分量自动分组**（Union-Find）+ **节点行样式升级**（类型徽章/状态徽章/hover 操作/选中高亮）。

## 拆分目标结构（新建 `components/right-panel/`）

```
components/right-panel/
├─ index.jsx              # 原 RightPanel 的 Tabs 容器壳（TabsList + 4 个 TabsContent），仅做组装
├─ search.js              # 共享：getPinyinKeys/matchText/matchNode/SearchBar 组件 + SearchBar
├─ constants.js           # 共享：NODE_CATEGORIES / ADD_ITEMS / EXECUTABLE_TYPES / MIN_CARD_WIDTH 等常量
├─ AddNodeTab.jsx         # 新增节点 tab：AddNodeList + AddNodeCard
├─ NodeManageTab.jsx      # 【重写】节点管理 tab：连通分量分组 + 升级版节点行
├─ HistoryTab.jsx         # 生成记录 tab：HistoryList + HistoryCard + HistoryImageThumb + formatTime
└─ (AssetLibrary 仍在 components/AssetLibrary.jsx，不挪)
```

原 `RightPanel.jsx` 改为从 `./right-panel` re-export default（保持 Canvas.jsx 的 `import RightPanel from './RightPanel'` 零改动），**或** 直接改成 re-export 一行。最终选择后者：`RightPanel.jsx` 留一行 `export { default } from './right-panel'`，避免遗留旧代码。

## NodeManageTab.jsx 详细设计

**新增能力**（参考 workflow-node-list-panel.tsx）：
1. **连通分量自动分组**（Union-Find）：把 `nodes` + `edges` 跑一次连通分量，size>1 的分组用色板（blue/emerald/amber/purple/rose/cyan/orange/pink 共 8 色）渲染成色块；size==1 的孤立节点归入「未连接」灰块。算法从参考文件 1:1 移植（纯 JS，无 TS 依赖）。
2. **节点行升级**（`NodeRow` 子组件）：第一行 `图标 / 标签名 / 类型徽章 / hover显示定位(Crosshair)+删除(Trash2)`；第二行（有内容时）显示状态徽章（生成中=amber、出错=red、产出张数）。整行点击 `onSelectNode`，hover 高亮 `bg-accent`，选中态 `bg-accent`。

**保留**：顶部 `SearchBar`（拼音搜索，复用 search.js）。

**数据来源**：`nodes`（已有）+ 新增 `edges`（Canvas 当前未传，需补）。

## Canvas.jsx 改动（仅 1 处，补 props）

第 907-924 行的 `<RightPanel>` 加两个 prop：
```jsx
<RightPanel
  nodes={nodes}
  edges={edges}              // 新增：传给连通分量算法
  selectedNodeId={selectedId} // 新增：高亮当前选中节点
  ...其余不变
/>
```
`edges` 已在 Canvas 解构（line 81），`selectedId` 已是 state（line 93），零新增状态。

## 兼容性 / 约束遵守
- **保留 4 个 tab 顺序**（add/nodes/history/assets）和图标不变。
- **保留所有现有交互**：新增节点的拖拽/执行 ⚡、历史记录的拖拽建节点/图片预览/素材库、AssetLibrary 不动。
- **节点管理交互不变**：定位/删除回调签名不变，只是 UI 升级。
- **遵守 handoff 约束 #3**：节点行内所有可交互元素加 `nodrag nopan nowheel`。
- **遵守约束 #11**：列表用 ReactFlow 之外的 DOM，不涉及 NodeToolbar。
- **不引入新依赖**：Union-Find 纯算法，图标全部来自 `@agent-spaces/ui`（已确认 8 个图标可用：Crosshair/Trash2/Play/Lock/Unlock/Check/X/Pencil 等）。
- **刷新即生效**：纯 mini-app src 改动，无需重启 web。

## 文件改动清单
| 文件 | 操作 |
|---|---|
| `components/right-panel/search.js` | 新建（迁移 SearchBar + 3 个匹配函数） |
| `components/right-panel/constants.js` | 新建（迁移 NODE_CATEGORIES/ADD_ITEMS/EXECUTABLE_TYPES/MIN_CARD_WIDTH） |
| `components/right-panel/AddNodeTab.jsx` | 新建（迁移 AddNodeList + AddNodeCard） |
| `components/right-panel/NodeManageTab.jsx` | 新建（**重写**：连通分量 + NodeRow） |
| `components/right-panel/HistoryTab.jsx` | 新建（迁移 HistoryList + HistoryCard + HistoryImageThumb + formatTime） |
| `components/right-panel/index.jsx` | 新建（Tabs 容器壳） |
| `components/RightPanel.jsx` | 改为 `export { default } from './right-panel'`（1 行） |
| `components/Canvas.jsx` | 补 `edges` + `selectedNodeId` 两个 prop（2 行） |

## 验收步骤
1. 刷新画布页面 → 右侧面板 4 个 tab 切换正常、内容不丢。
2. 「新增节点」tab：搜索/分类筛选/拖拽建节点/可执行节点 ⚡ 仍可用。
3. 「节点管理」tab：连线的节点自动归到同色块分组，无连线节点在「未连接」分组；节点行显示类型徽章、状态徽章、hover 出现定位/删除按钮。
4. 「生成记录」tab：历史卡片/拖拽建节点/图片预览/清空 不变。
5. 选中画布某节点 → 列表对应行高亮。
6. 「素材库」tab 不受影响。