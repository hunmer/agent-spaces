{
  "name": "@agent-spaces/tauri",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "dev:macos": "tauri dev --target aarch64-apple-darwin",
    "dev:windows": "tauri dev --target x86_64-pc-windows-msvc",
    "dev:linux": "tauri dev --target x86_64-unknown-linux-gnu",
    "dev:ios": "tauri ios dev --target aarch64",
    "dev:android": "tauri android dev --target aarch64",
    "build": "tauri build",
    "build:macos": "tauri build --target aarch64-apple-darwin",
    "build:windows": "tauri build --target x86_64-pc-windows-msvc",
    "build:signed_ios": "tauri ios build --target aarch64 --ci",
    "build:ios": "tauri ios build --target aarch64 --no-sign --ci",
    "build:android": "tauri android build --target aarch64",
    "build:android:debug": "tauri android build --debug --target aarch64"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.0",
    "@tauri-apps/plugin-notification": "^2"
  }
}
