# Coss UI registry

These files are downloaded from the official Coss registry source:

https://github.com/cosscom/coss/tree/main/apps/ui/registry/default

The components are kept under `ui/coss` while the application is migrated incrementally. Existing imports from `@/components/ui/*` continue to use the current compatibility layer until each component's API is verified.
