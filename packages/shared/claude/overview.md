# Shared 模块 — 概述

## 类型文件清单 (src/types/)

agent.ts, channel.ts, code-favorites.ts, command.ts, database.ts, events.ts, file.ts, git.ts, hooks.ts, index.ts, issue.ts, knowledge-base.ts, llm.ts, notification.ts, search.ts, speech.ts, sqlite.ts, subscription.ts, **team.ts**, task.ts, tool.ts, workflow-composite.ts, workflow-errors.ts, workflow-execution.ts, workflow-node-factory.ts, workflow-plugin.ts, workflow-shortcut.ts, workflow-ws.ts, workflow.ts, workspace.ts, worktree.ts

共 30 个类型文件，覆盖所有领域模型。Team 协作类型见 `team.ts`（Team / TeamMembership / TeamMessage / TeamInboxItem / TeamRuntime / TeamComment + 角色与状态枚举）。
