---
sidebar_position: 1
slug: /
---

# Agent Spaces

Agent Spaces 是一个**可视化工作流自动化平台**。用 DAG 拖拽编排工作流，混合调度 AI Agent、代码、数据库、知识库、人机交互与 Mini App 界面节点，把重复的 AI 任务沉淀成可复用、可触发、可观测的自动化流程。

核心是**节点化的可视化工作流引擎**：AI 协同、代码执行、Mini App 界面都只是工作流里的一类节点，而非独立产品。一个工作流可以由定时任务、Webhook、HTTP API、Issue 事件或 Agent 工具调用触发，也可以作为子流程嵌套进别的工作流。

## 为什么选择 Agent Spaces？

- **可视化工作流引擎（核心）** — 基于 @xyflow/react 的 DAG 编辑器，拖拽 40+ 内置节点连线定义执行流，替代硬编码 pipeline
- **9 大节点分类** — 流程控制 / AI / 人机交互 / 展示 / SQLite 数据库 / 知识库 / 工具与字符串 / Mini App / 插件自定义节点
- **复合节点与子流程** — loop+loop_body 循环（支持并发）是复合节点，sub_workflow 可嵌套调用其他工作流
- **多种触发方式** — cron 定时、Webhook、HTTP API（SSE 流式）、WebSocket 实时、Issue 自动编排、Agent 工具调用
- **多种执行/输出形态** — WebSocket 事件流、HTTP SSE 流式响应、Webhook SSE 回调、断点调试、暂停/恢复/停止、断线快照恢复
- **六种 AI 运行时** — Claude Code / OpenAI Codex / LangChain / Open Agent SDK / Hermes / Oh-My-Pi，一键发现/安装/更新
- **Mini App 交互节点** — 独立轻量应用子系统，可单独运行，也可通过 show_miniapp 节点嵌入工作流阻塞收集用户数据
- **完全本地运行** — 代码不离开你的机器，JSON 文件 + SQLite 持久化，无需外部数据库
- **IDE 级前端 + 多端客户端** — Monaco 代码编辑器（TypeScript LSP）、终端、Git、Web + Electron + Flutter
- **实时通知与远程操控** — 飞书/企业微信 Bot 推送 + Native 通知，Bot 斜杠命令远程操控

## 核心能力

### 工作流引擎（核心）

| 能力 | 说明 |
|------|------|
| 可视化 DAG 编排 | @xyflow/react 拓扑编辑器，@dagrejs/dagre 自动布局 |
| 流程控制节点 | start / end / switch 分支 / loop 循环（并发）/ variable_aggregate 汇聚 / set·get·delete_variable / run_code(JS) / run_python / loop_break |
| AI 节点 | agent_run 调用 Agent（六种角色 × 六种运行时，含权限模式 default/dontAsk/acceptEdits/plan/auto/bypassPermissions） |
| 人机交互节点 | alert / prompt 输入 / form 表单（阻塞等待用户回应） |
| 展示节点 | gallery_preview 图库 / music_player / table_display / code_render(React/HTML) / markdown / sticky_note / file_display |
| SQLite 数据库节点 | query / insert / update / delete / raw（含 SQL 安全校验） |
| 知识库节点 | kb_add / kb_query / kb_delete（向量检索） |
| 工具/字符串节点 | flatten_array / pluck_array_key / merge_arrays / parse_json / string_concat / string_split |
| Mini App 节点 | show_miniapp 在流程中弹出 Mini App 界面，阻塞收集用户提交数据 |
| 插件节点 | 插件可动态注册自定义节点（服务端/客户端/双端执行） |
| 复合节点与子流程 | loop 是 compound 复合节点（含作用域边界），sub_workflow 可嵌套调用其他工作流 |
| 触发方式 | cron 定时 / Webhook(hook) / HTTP API(SSE) / WebSocket 实时 / Issue 自动编排 / Agent 工具调用 |
| 执行/输出形态 | WebSocket 事件流 / HTTP SSE 流式 / Webhook SSE 回调 / 断点调试 / 暂停恢复停止 / 断线快照恢复 |
| 工程化能力 | 分组(group) / 版本管理 / 发布开关(published) / 工作流级变量 / 容错模式(ignore/stop) / 节点断点 / 节点禁用跳过 / 执行日志 |

### 其他能力

| 能力 | 说明 |
|------|------|
| 工作空间管理 | 绑定本地代码目录，支持文件夹浏览和 Git Clone |
| 代码编辑器 | Monaco Editor + TypeScript LSP（定义跳转/引用/诊断），代码收藏，代码搜索 |
| 终端 | 集成终端，支持快捷命令（自定义命令 + 自动重启） |
| 议题管理 | Issue 绑定 Workflow 模板，自动编排执行，评论时间线、失败重试、状态流转 |
| 频道聊天 | @mention Agent 触发对话和任务执行，TipTap 富文本，语音识别输入 |
| Git 操作 | 可视化分支管理、提交、合并，Worktree 并行开发，Git 操作日志 |
| Agent 角色与运行时 | 六种角色（agent/scheduler/task_creator/bot + 自定义）× 六种运行时 |
| 运行时管理 | 一键发现/安装/更新本地 CLI 与 SDK（claude-code/codex/gemini-cli/hermes/pi 等） |
| Hook 系统 | Agent 工具调用前后自定义钩子（24 种事件），per-tool-call 粒度，shell/webhook/script 动作 |
| 输出风格 | 自定义 Markdown 输出格式模板，注入 Agent systemPrompt |
| 通知中心 | 飞书/企微/Native/应用内消息推送，Robot Account 集中凭证管理 |
| Kanban 看板 | @dnd-kit 拖拽排序，多列管理，优先级筛选，水平/垂直布局切换 |
| 文档数据库 | Notion 风格树形文档，Notion/Markdown 双编辑器，向量搜索，版本历史 |
| Worktree | Git Worktree 并行开发，Diff 查看，AI PR 描述生成，PR 合并 |
| 用量统计 | Token 消耗、费用估算、趋势图表、按模型/会话统计 |
| Plugin 系统 | 120+ 插件模板，扩展 Agent 工具能力（云存储/多媒体/邮件等） |
| Skill 系统 | 66+ 技能模板，为 Agent 注入方法论（superpowers/tdd/planning 等） |
| 代码搜索 | ripgrep 优先 + Node.js 回退，支持正则/文件模式/大小写 |
| LLM 管理 | 多模型配置、Anthropic Bridge 协议中转 |
| Agent SSE API | HTTP POST 流式调用 Agent，支持外部集成 |
| Command Palette | Ctrl+K 快捷面板，全局搜索工作空间/频道/Issue/文件/服务器 |
| 认证系统 | Secret Key + Bearer Token 认证 |
| i18n 国际化 | 中英文切换 |

## 下一步

准备好开始了吗？前往 [安装部署](/docs/getting-started/installation) 搭建你的 Agent Spaces 环境。
